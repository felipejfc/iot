# Pro Micro nRF52840 board config - no DK library (no LEDs/buttons infrastructure)
# We use custom interrupt-based button handling in button_handler.c
CONFIG_DK_LIBRARY=n

# Force disable debug features for low power
CONFIG_PRINTK=n
CONFIG_ASSERT=n
